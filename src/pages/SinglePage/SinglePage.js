import React from "react";
import useStyles from "./styles";
const SinglePage = () => {
  const classes = useStyles();
  return <section className={classes.singlePage}>SinglePage</section>;
};

export default SinglePage;
